# Legacy / rollback

`v2-worker.js` is the prior working Worker source kept only as a rollback/reference copy.

The v3 Worker source of truth is:

`worker/src/index.js`

Do not put API keys or passwords in either file.
